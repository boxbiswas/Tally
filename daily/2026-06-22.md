# TALLY — Daily Documentation

Date: 2026-06-22

Summary
-------
- Short record of work completed on the TALLY project today.

Backend
-------
- Applied Prisma migration: `npx prisma migrate dev` (ran in `backend` terminal; exit code 0).
- Updated/verified Prisma config and client in `backend/config/prisma.js` and `backend/prisma.config.ts`.
- Migrated schema source: `backend/prisma/schema.prisma`.
- Confirmed server wiring and auth middleware in `backend/app.js` and `backend/middleware/authMiddleware.js`.
- Worked on/updated controllers: `backend/controllers/authController.js`, `backend/controllers/companyController.js`, `backend/controllers/ledgerController.js`, `backend/controllers/purchaseController.js`, `backend/controllers/salesController.js`, `backend/controllers/stockController.js`.

Frontend
--------
- Checked frontend scaffolding and entry points: `frontend/src/main.jsx`, `frontend/src/App.jsx`.
- Styles and assets reviewed: `frontend/src/index.css`, `frontend/App.css`.
- Project config: `frontend/package.json`, `frontend/vite.config.js`, `frontend/index.html`.

Files changed (to review)
-------------------------
- `backend/config/prisma.js`
- `backend/prisma.config.ts`
- `backend/prisma/schema.prisma`
- Controller files under `backend/controllers/` (auth, company, ledger, purchase, sales, stock)
- `frontend/src/main.jsx`, `frontend/src/App.jsx`

Dev commands (run locally)
--------------------------
Apply migrations / open Prisma Studio:

```bash
cd backend
npx prisma migrate dev
npx prisma studio
```

Start servers (examples):

```bash
cd backend
node app.js

cd frontend
npm run dev
```

Resume notes
------------
- Re-read the daily changelog and verified the last completed milestone was the Prisma migration plus schema/config updates.
- Exact stop point: backend implementation had not yet moved past the scaffolded API layer; the server entry point, controllers, routes, and auth middleware were still in a pending/unfinished state.
- Current status after review: the Prisma schema layer is in place, but backend route/controller implementation and frontend integration work remain pending.
- Next step when resuming: continue from the backend API surface instead of repeating the migration/schema work, then update the daily log with only the newly completed changes.


Author: Indrasish
